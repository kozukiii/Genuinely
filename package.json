{
  "name": "price-sorting-bot",
  "private": true,
  "scripts": {
    "dev": "concurrently -k --prefix \"[{name}]\" --timestamp-format \"HH:mm:ss\" -n api,web -c magenta,cyan \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
